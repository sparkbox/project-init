(function () {
	if (Modernizr.fontface) {
		doSomething();
	}

	if (!Modernizr.svg) {
		doSomething();
	}

	if (Modernizr.contenteditable && !Modernizr.csstransforms3d) {
		doSomething();
	}

	if (Modernizr.overflowscrolling&&Modernizr.contextmenu) {
		doSomething();
	}

	if (Modernizr.scriptdefer===true) {
		doSomething();
	}

	if (Modernizr.fontface !== true) {
		doSomething();
	}
}());
